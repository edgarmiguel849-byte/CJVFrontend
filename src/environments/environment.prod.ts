// PRODUCCIÓN (Angular ya empaquetado dentro del JAR).
// Ruta relativa: el navegador llama al MISMO host:puerto desde donde
// cargó la página. Funciona en la laptop-servidor sin tocar nada.
export const environment = {
  production: true,
  apiUrl: '/api',
};
